meta {
  name: Invalid Token
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/auth/test-auth
  body: none
  auth: bearer
}

auth:bearer {
  token: invalid_token
}

assert {
  res.status: eq 403
}
