meta {
  name: Valid Token
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/auth/test-auth
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.message: eq Authenticated
}
